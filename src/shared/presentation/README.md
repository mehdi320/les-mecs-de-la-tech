# shared / presentation

Layout general, navigation entre modules, ecran de facturation/palier,
composants d'authentification. Les composants UI reutilisables
purement visuels vivent dans `src/shared/ui/`, pas ici.
