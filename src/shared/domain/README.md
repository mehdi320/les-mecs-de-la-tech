# shared / domain

Entites et regles transverses aux quatre modules : `Client` (tenant),
notion de `plan`/palier tarifaire, interfaces de repository generiques.
Aucun module ne doit dupliquer la notion de `Client` ou de tenant.
