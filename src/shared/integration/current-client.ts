/**
 * L'authentification multi-client n'est pas dans le perimetre de
 * cette session de code : toutes les pages operent sur le client de
 * demonstration seede par `npm run db:seed`. A remplacer par la
 * session utilisateur authentifiee quand l'auth sera implementee.
 */
export const CLIENT_ID_COURANT = "demo-client";
