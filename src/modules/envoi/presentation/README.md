# Envoi / presentation

Routes Next.js App Router (pages et handlers `api/`) et composants
UI propres au module Envoi : connexion de mailbox, constructeur de
sequences, ecran de creation/suivi de campagne.

Ne contient aucune logique metier ni acces direct aux donnees : passe
toujours par les cas d'usage exposes depuis `domain/`.
