# db

SQLite pour le MVP. `migrations/` porte le schema versionne
(tables listees en SPEC.md section 3, avec `client_id` sur chaque
table portant des donnees de contact). `seed/` porte des donnees de
demo pour le developpement local, jamais de donnees de contact reelles.
